#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
   float c;
   float f;
   printf("nhap do c : ");
   scanf("%f", &c);
   f =9/5*c+32;
   printf("do f tuong ung : %0.1f", f);
	return 0;
}
