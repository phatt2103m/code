#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float dola;
	printf("so tien muon doi : ");
	scanf("%f", &dola);
	dola = dola * 16.189;
	printf("%f", dola);
	return 0;
}
